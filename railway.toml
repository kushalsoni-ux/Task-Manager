[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm --prefix frontend run preview -- --host 0.0.0.0 --port 3000"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
